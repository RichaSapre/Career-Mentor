# API base URL (include full path to your backend)
# Example: http://localhost:4300/alpha/api/v1
NEXT_PUBLIC_API_BASE_URL=http://localhost:4300/alpha/api/v1

# Set to "true" to use mock data instead of real API (for UI demo)
NEXT_PUBLIC_DEMO_MODE=false
